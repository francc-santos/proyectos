public class OuterClass {

    /* Aveces se necesita que una propiedad sea un objeto. El ejemplo es cunando se quiere definir una maquina virtual, cuando necesitamos definir la class maquina virtual necesitamos definir memoria, procesador, y con un atributo no se pude crear un procesador y aveces teenr afuera la clase eleva la complejidad. */

	class NestedClass {
        // una clase dentro de una clss , es una clse anidada
	}

    // 
	static class StaticNestedClass {
        // 
	}

    // 
	class InnerClass {
        // Es cuando la clss no es estatica , que no esta anidada 
	}
}